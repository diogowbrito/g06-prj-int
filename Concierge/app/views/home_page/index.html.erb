
  <table>
    <tbody>
    <% counter = 0 %>
    <% @services.each do |service| %>
        <% if counter % 4 == 0 then %>
            <tr>
        <% end %>
        <td>
         <img src="/images/letra-c.png" id="serviceLink" href=<%="http://localhost:3000/services/"+service.serviceName+"/index"%>>
         <p><%= service.serviceName %></p>
         <!--  <%= link_to image_tag("/images/letra-c.png"), "http://localhost:3000/services/"+service.serviceName+"/index", :id => "serviceLink" %> -->
        </td>
        <% counter = counter + 1 %>
        <% if (counter % 4 == 0) || (counter == @services.size) then %>
            </tr>
        <% end %>
    <% end %>
    </tbody>
  </table>