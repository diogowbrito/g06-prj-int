<%= form_for(@ref_entity) do |f| %>
  <% if @ref_entity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ref_entity.errors.count, "error") %> prohibited this ref_entity from being saved:</h2>

      <ul>
      <% @ref_entity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :serviceName %><br />
    <%= f.text_field :serviceName %>
  </div>
  <div class="field">
    <%= f.label :entity %><br />
    <%= f.text_field :entity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
