<%= form_for(@course_degree) do |f| %>
  <% if @course_degree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_degree.errors.count, "error") %> prohibited this course_degree from being saved:</h2>

      <ul>
      <% @course_degree.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.text_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :degree_id %><br />
    <%= f.text_field :degree_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
